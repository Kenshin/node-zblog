
exports.zblog_config = function( host, port, path, user_name, password ) {
    this.host = host;
    this.port = port;
    this.path = path;
    this.user_name = user_name;
    this.password = password;
}
